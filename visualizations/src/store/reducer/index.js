import {

} from '../actions';

const initialState = {

};

const reducer = (state = initialState, action) => {
  switch(action.type) {
    // case FETCH_START:
    //   return {
    //     ...state,
    //     fetching: true,
    //     error: ''
    //   }
    default:
      return state
  }
}

export default reducer;
